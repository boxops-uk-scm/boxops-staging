import * as internal from './StatusDot';

const StatusDot = Object.assign(internal.default, {
  styles: internal.styles,
  variants: internal.variants,
});

namespace StatusDot {
  export type Props = internal.Props;
}

export default StatusDot;
