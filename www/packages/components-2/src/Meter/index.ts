import * as internal from './Meter';

const Meter = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Meter {
  export type Props = internal.Props;
  export type Status = internal.Status;
}

export default Meter;
