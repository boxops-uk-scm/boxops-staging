import * as internal from './RichTextArea';

const RichTextArea = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace RichTextArea {
  export type Props = internal.Props;
}

export default RichTextArea;
