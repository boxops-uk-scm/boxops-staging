import * as internal from './DateTime';

const DateTime = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace DateTime {
  export type Props = internal.Props;
}

export default DateTime;
