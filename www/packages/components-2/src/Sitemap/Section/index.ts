import * as internal from './Section';

const Section = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Section {
  export type Props = internal.Props;
}

export default Section;
