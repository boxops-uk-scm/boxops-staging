import * as internal from './Initials';

const Initials = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Initials {
  export type Props = internal.Props;
}

export default Initials;
