import * as internal from './Divider';

const Divider = Object.assign(internal.default, {
  styles: internal.styles,
  variants: internal.variants,
});

namespace Divider {
  export type Props = internal.Props;
}

export default Divider;
