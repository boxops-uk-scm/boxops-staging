import * as internal from './Header';

const Header = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Header {
  export type Props = internal.Props;
}

export default Header;
