import * as internal from './Footer';

const Footer = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Footer {
  export type Props = internal.Props;
}

export default Footer;
