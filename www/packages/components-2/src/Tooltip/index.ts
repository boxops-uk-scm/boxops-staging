import * as internal from './Tooltip';

const Tooltip = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace Tooltip {
  export type Props = internal.Props;
}

export default Tooltip;
