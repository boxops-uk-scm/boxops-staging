import * as internal from './PageTitle';

const PageTitle = Object.assign(internal.default, {
  styles: internal.styles,
});

namespace PageTitle {
  export type Props = internal.Props;
}

export default PageTitle;
